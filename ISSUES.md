# 麦肯锡高管商业图表套件：核心问题与缺陷清单 (Problem & Defect Catalog)

---

### 1. 案例数据严重污染与脏数据渗透 (Mock Data Pollution & Leakage)
* **硬编码业务假数据**：模板引擎与图表构建器中大量写死了民航案例的固定内容（如“公务出行”、“休闲度假”、“A350/B787机型”、“夏秋航季”、“华东基地”、“可用座公里 ASK”、“周转客公里 RPK”、“套保收益 +¥0.8亿”等）。
* **跨行业脏数据残留**：在生成零售、SaaS、金融等非航空行业看板时，上述民航假数据依然在图表、Tooltip、指标卡及副标题中顽固出现。
* **过滤逻辑写死转换字典**：筛选联动中写死了 `hubMap` 字典，一旦遇到非民航大区，直接回退并覆盖出错误的民航假算法数据。

---

### 2. 筛选器被死死写死为 4 个坑位 (Rigid 4-Slot Filter Anti-Pattern)
* **固定坑位无法适配真实维度**：顶部筛选栏死守 4 个位置（时段、枢纽、机型、航线），无法支持真实业务中 0 到 N 个任意维度（例如零售只有 3 个维度：周期、大区、状态；部分报表只有 1 个或需要 5 个维度）。
* **假通用**：仅仅修改 4 个标签文字（labels），底层 DOM、状态管理和过滤算法依然写死为 4 个固定字段。
* **维度不匹配时界面异常**：维度少于 4 个时出现无意义的空白死坑位，超出 4 个维度时无处安放。

---

### 3. 表头与数据量纲缺失 (Missing Explicit Metric Units)
* **表头无单位引起歧义**：数据透视表与图表转换表格的表头没有标注单位（缺少 `(万元)`、`(%)`、`(元/㎡)`、`(天)` 等）。
* **管理层无法判断量纲**：用户无法直观辨识数值是“元”还是“万元”，是“绝对量”还是“相对比率”，极易导致汇报误判。

---

### 4. 数据清洗与格式化一刀切破坏数据 (Aggressive & Destructive Data Formatting)
* **非金额指标被暴力格式化为货币**：多维筛选联动时，所有数值被一律套用金额清洗逻辑，导致毛利率（如 32.3%）、库存天数（如 40.6 天）、客单价等被强制洗成 `¥0.00 亿`。
* **数据异常崩溃**：当单元格包含文字或非标准符号时，格式化逻辑直接产生 `NaN` 或破损字符串。

---

### 5. 色彩设计脱离规范，出现刺眼彩虹色 (Garish Colors & Missing Palette Reference)
* **回退到原生彩虹色**：饼图、环形图、多系列柱状图没有正确引用调色盘，直接呈现 ECharts 自带的荧光蓝、浅绿、明黄、橙红等刺眼杂乱色彩。
* **缺少业务语义色**：正向增长、负向承压、重点预警缺乏明确的语义色区分；散点图所有点均为单一蓝色，无法区分梯队。
* **缺乏统一的高管质感**：颜色饱和度过高，破坏了严肃商业报告的沉稳与专业感。

---

### 6. 图表系统分类代码 Badge 未显示 (Missing System Taxonomy Badges)
* **卡片缺少代号标识**：图表卡片 Header 处没有渲染其专业分类编号（如 `C01`, `T06`, `R01`, `K01`），无法对应到 52 款标准图表库。
* **顶层元数据缺省破损**：页面顶层 `org`（机构）或 `system`（系统）未传时，界面直接显示空白或 `undefined`。

---

### 7. Agent 频繁翻阅与抄写 4000 行 HTML 源码 (Agent Copying Raw HTML Anti-Pattern)
* **大量消耗 Token 且极易截断**：Agent 倾向于直接查看 `examples/*.html` 并手写 3000~4000 行巨大 HTML，极易发生语法截断与 DOM 破损。
* **复制旧代码中的脏数据**：Agent 在抄写过程中，将样例中残留的业务假数据和死逻辑完整复制到新页面中。

---

### 8. “黑盒一键出图”导致特制化与极度脆弱 (Blackbox Guessing & Ad-Hoc Hardcoding)
* **死代码猜测业务逻辑**：试图用脚本黑盒自动猜测 CSV 内容，导致代码中充斥针对特定列名（如“广州天河”、“坪效”、“门店”）的硬编码正则。
* **特制化陷阱**：脚本沦为仅适用于某一份特定 CSV 的特制代码，面对新行业或新表格结构时极易崩溃或产出错误图表。
* **剥夺 AI 分析能力**：把属于 AI 的业务理解、指标提取与图表选型职责错误地交给了死逻辑。
