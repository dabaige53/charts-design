# 麦肯锡高管商业图表套件：核心问题集与架构治理复盘报告
> Executive Charts Governance, Defect Postmortem & Architectural Evolution

---

## 0. 背景与治理目标

在企业级经营分析看板与商业图表套件的早期演进中，系统暴露出了一系列**数据污染、结构固化、色彩失真、AI 行为失范以及黑盒猜测带来的业务脆弱性**。

本文档系统性归纳并复盘了该套件在实际应用中暴露的 **7 大核心问题集**、根本原因、风险影响、治理改造方案以及最终确立的工程规范，作为后续开发、迭代与 AI 协同的标准参考。

---

## 1. 核心问题集深度归因与治理复盘

### 🔴 问题 1：案例脏数据严重污染与渗透 (Mock Data Pollution & Leakage)

* **缺陷现象**：
  在为零售、SaaS、金融等非航空行业生成看板时，图表、Tooltip 及 KPI 指标卡中大量残存民航案例数据（如出现“公务出行/休闲度假”、“A350/B787机型”、“夏秋航季”、“华东基地”、“可用座公里 ASK”、“周转客公里 RPK”、“套保收益”等）。
* **根本原因**：
  1. 模板引擎 (`dashboard_template.py`) 和图表构建器 (`chart_builders.py`) 中直接硬编码了大量默认业务数据；
  2. 联动过滤逻辑 (`updateKPIsAndChartsFromFilters`) 中硬编码了 `hubMap` 转换字典，并在未匹配时暴力回退到民航算法；
  3. 渲染骨架与业务数据强耦合，未实现“零数据纯净引擎”。
* **风险与影响**：
  高管看板作为战略决策工具，出现不相干行业的脏数据会导致业务认知混乱，彻底破坏报告的可信度与专业度。
* **治理方案**：
  * 彻底清除模板中所有硬编码业务字串与假算法；
  * 构建 **“零数据纯净 BI 渲染骨架 (Zero-Data BI Engine)”**，未传入业务数据时展示纯净空网格，绝对禁止任何隐式回退；
  * 联动过滤引擎改为通用属性扫描与键名直连，杜绝硬编码字典。

---

### 🔴 问题 2：固化 4 坑位筛选器假通用 (Rigid 4-Slot Filter Anti-Pattern)

* **缺陷现象**：
  顶部筛选栏被写死为 4 个固定位置（时段、枢纽、机型、航线），无法适应不同行业的多样化维度（例如零售行业只需要 3 个维度：统计周期、所属大区、经营状态；其他行业可能有 2 个或 5 个维度）。
* **根本原因**：
  UI 结构与状态管理器 (`StateStore`) 写死了 4 个维度的 DOM 结构与属性读取逻辑，所谓的“通用”只是改了 4 个标签文字的假通用。
* **风险与影响**：
  强迫业务数据去适配固定的 4 坑位，导致多余的筛选器显示为无效选项，或者维度不足时出现空白坑位。
* **治理方案**：
  * 将筛选器升级为全动态数组契约：`filters: [ { key, label, type, options }, ... ]`；
  * 支持 `select`（单选）、`multi-select`（多选搜索）等任意类型；
  * 传 0 到 N 个维度自适应渲染，支持动态计算已选数量 Badge 与一键重置。

---

### 🔴 问题 3：表头与图表量纲缺失及暴力数据清洗 (Missing Explicit Units & Aggressive Formatting)

* **缺陷现象**：
  1. 数据透视表与图表转换表格中，表头缺少量纲（如只写“销售额”、“毛利率”、“坪效”，缺少 `(万元)`、`(%)`、`(元/㎡)`、`(天)`），引起管理层阅读歧义；
  2. 在多维筛选联动时，非金额类指标（如百分比、天数、客单价）被格式化函数暴力清洗为 `¥0.00 亿`。
* **根本原因**：
  1. 列定义缺乏标准量纲规范 (`unit`)；
  2. 数据清洗函数对所有数值统一执行金额正则（`¥... 亿`），缺乏对指标类型的判断。
* **风险与影响**：
  量纲歧义会导致汇报产生严重误判（如万元看成元）；暴力清洗将正常比率或天数破坏为错误货币，导致看板数据失效。
* **治理方案**：
  * 在列定义与图表提取器中强制注入 `unit` 属性（如 `unit: "万元"`、`unit: "%"`），并在表头显式渲染；
  * 引入基于指标类型（金额、比率、工期天数、计数）的智能格式化规约，杜绝统一暴力刷金额。

---

### 🔴 问题 4：色彩体系失真与 ECharts 默认彩虹色 (Color System Violation & Garish Rainbows)

* **缺陷现象**：
  饼图、环形图、多系列柱状图在渲染时呈现出 ECharts 自带的刺眼荧光蓝、浅绿、明黄、橙红等“默认彩虹色”，散点图所有点均为单一蓝色，完全脱离麦肯锡咨询级审美。
* **根本原因**：
  `TOKENS` 中未在 `commonOption.color` 注入全局定性调色盘，导致 ECharts 在未指定颜色时自动回退到自带默认色系；部分图表构建函数缺少对调色盘 Token 的显式引用。
* **风险与影响**：
  杂乱无序的彩虹色破坏了高管报告沉稳、严谨、专业的基调，视觉噪点严重干扰决策研判。
* **治理方案**：
  * 全局注入麦肯锡 8 色定性/定量调色序列：
    `['#123B5D', '#2C6485', '#628EA8', '#8C9DAE', '#9A6A18', '#A4453C', '#2F6B55', '#BDD0DC']`；
  * 严格落实业务语义色：正向增长/达标为墨绿 `#2F6B55`，负向承压/预警为绯红 `#A4453C`，重点关注为琥珀金 `#9A6A18`；
  * 四象限散点图按领跑（深海蓝）、新星（墨绿）、存量（琥珀金）、预警（绯红）智能着色。

---

### 🔴 问题 5：图表系统分类代码 Badge 未显式展示 (Missing System Taxonomy Badges)

* **缺陷现象**：
  图表卡片 Header 处没有显示其专业分类代码（如 `C01`, `T06`, `R01`, `K01`），无法与 52 款麦肯锡图表库形成标准化对应。
* **根本原因**：
  看板模板在渲染图表 Header 时遗漏了 `${chart.code.toUpperCase()}` 的 Badge 标签。
* **风险与影响**：
  降低了系统工程化与规范化程度，用户与 AI 无法快速溯源该图表的分类学定义与标准用法。
* **治理方案**：
  * 在每个图表卡片标题右侧统一挂载等宽字体代码标签：
    `<span class="font-mono text-[10px] font-bold uppercase bg-slate-100 text-slate-600 border border-slate-200">C01</span>`；
  * 顶层系统标题元数据完善，处理 `org` 与 `system` 缺省回退，杜绝出现空白或 `undefined`。

---

### 🔴 问题 6：Agent 频繁阅读与抄写 4000 行 HTML 源码的反模式 (Agent Copying Raw HTML Anti-Pattern)

* **缺陷现象**：
  AI Agent 在收到画图或做看板任务时，倾向于直接打开 `examples/*.html` 查阅，并试图在当前对话中从头手写 3000~4000 行包含大量 CSS/JS 的单体 HTML。
* **根本原因**：
  1. 缺少清晰的高层 CLI / API 工具抽象；
  2. `SKILL.md` 缺少对 Agent 行为的硬性约束（Guardrails）。
* **风险与影响**：
  手写数千行 HTML 极易发生语法截断、DOM 遗漏、样式冲突，且极大概率复制样例文件中的残存业务假数据。
* **治理方案**：
  * 在 `SKILL.md` 中立下**绝对铁律**：严禁 Agent 阅读或仿写 `examples/*.html`；
  * 提供 `npx charts-design` CLI 与声明式 `generate_dashboard.py --config` 工具，Agent 仅需输出结构化业务 JSON 即可完成编译。

---

### 🔴 问题 7：“黑盒一键出图”的业务脆弱性与特制化陷阱 (Blackbox "from-csv" Guessing Trap)

* **缺陷现象**：
  过度追求黑盒式“从 CSV 一键直出完整看板”，导致脚本内部充斥着对特定 CSV 列名（如“广州天河”、“坪效”、“门店”）的硬编码正则与特殊处理，一旦换一个行业或表格结构就彻底失效。
* **根本原因**：
  混淆了**“工具职责”**与**“AI 职责”**，试图用死代码去硬猜千变万化的业务规律，剥夺了 AI Agent 的业务分析与洞察能力。
* **风险与影响**：
  黑盒出图稳定性极差，无法适应真实世界的复杂多变业务场景，沦为针对特定样例的特制化脚本。
* **治理方案**：
  * 确立 **“AI 驱动的模块化编排架构 (AI-Orchestrated Modular Architecture)”**：
    1. **原子图表组件 (`generate_chart.py`)**：负责高质量生成单个图表与批量组件 (`--batch`)；
    2. **AI Agent 作为业务编排者**：负责分析用户数据、提取业务指标、挑选 4~6 款最佳图表、编写精准口径说明并组装 JSON；
    3. **确定性看板引擎 (`generate_dashboard.py`)**：负责 100% 严格按照 AI 提供的结构进行渲染，0 猜测、0 假数据、0 特制代码。

---

## 2. 治理前后对比矩阵 (Before vs After Matrix)

| 维度 | 治理前 (Legacy) | 治理后 (v1.0.3) |
| :--- | :--- | :--- |
| **数据纯净度** | 充斥民航假数据（公务出行、机型、航季） | **100% 纯净数据驱动，0 业务假数据残留** |
| **筛选器架构** | 固化 4 坑位（时段、枢纽、机型、航线） | **全动态 N 维数组，传几个渲染几个，支持多选/单选** |
| **色彩规范** | ECharts 默认彩虹色（亮黄/浅绿/刺眼蓝） | **麦肯锡 8 色定性序列 + 严格业务语义色 (正绿负红)** |
| **量纲与单位** | 表头无单位，非金额指标被强洗成 `¥0.00 亿` | **显式携带 `unit` 并在表头注入，按指标类型智能格式化** |
| **图表系统代码** | 卡片无代号展示，缺乏分类溯源 | **标题右侧显式展示 `C01`、`T06`、`R01` 等宽代码 Badge** |
| **Agent 工作流** | 抄写 4000 行 HTML 源码，极易出现幻觉与截断 | **结构化 JSON 声明式生成，严禁手写巨型 HTML** |
| **系统架构模式** | 黑盒一键猜测出图（脆弱的死正则与特制代码） | **AI 模块化编排 + 确定性引擎编译（高稳定、强扩展）** |

---

## 3. 标准化设计与开发规范准则 (Engineering Standards)

1. **【零假数据铁律】**：模板与引擎中绝不出现任何具体业务实体名称（如航司、车企、电商名），所有文字必须通过 `config` 传入；
2. **【显式量纲铁律】**：所有数值字段必须声明 `unit`，表头格式为 `字段名 (单位)`，Tooltip 必须包含对应单位；
3. **【色彩令牌铁律】**：所有图表 series 必须绑定 `tokens.palette` 或 `tokens.commonOption.color`，严禁使用原生彩虹色；
4. **【代码标签铁律】**：所有图表卡片 Header 必须挂载 52 分类学代码；
5. **【AI 职责边界】**：AI 负责业务计算与配置编排，CLI / Python 工具负责遵循规范确定性渲染。
